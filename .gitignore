# Dependencies
node_modules
/client/node_modules
/client/build
/client/public/assets
/client/public/media
# Per-server active theme only — theme definitions stay in git
routes/themes/active-theme.json
# Sensitive config — contains DB creds, JWT secret, URLs
config.json
.env
client/.env

# OS / logs
.DS_Store
__MACOSX/
*.log